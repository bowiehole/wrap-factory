# volt-rail-v2

UV remap remaster of `volt-rail` (same motif as v1 — not a new invent).

**Why:** Shared-span UV fix for Model 3 / Y / S / X column atlases (island height for u, island width + per-row roofward edge for v). Cybertruck already used shared rail height; remastered with the same mapper.

**Brief:** same graphite lime/cyan power-rails; remastered UV

**Date folder:** `2026-09-07` (original drop date kept; v1 files untouched).
**Generator:** `/workspace/wrap-preview/tools/skin-volt-rail.js` via `--template/--out/--folder` (never `--all`).
