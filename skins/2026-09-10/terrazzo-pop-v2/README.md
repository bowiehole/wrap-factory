# terrazzo-pop-v2

WORLD-BAKE wrap-around remaster of `terrazzo-pop` (same motif as v1 — not a new invent).

**Why:** World-space bake from GLB `(along, height)` so terrazzo chips flow continuously across hood, sides, and rear — wrap-around continuity rather than shared-span UV island remapping.

**Brief:** same warm parchment multi-color terrazzo chips; world-bake wrap-around remaster

**Date folder:** `2026-09-10` (original drop date kept; v1 `terrazzo-pop.png` files untouched).
**Bake:** GLB world `(along=nose→tail, heightV=roof→rocker)` via `bake-world-field.mjs` / seed 0 on every panel.
