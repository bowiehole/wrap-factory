# reef-flash-v2

WORLD-BAKE wrap-around remaster of `reef-flash` (same motif as v1 — not a new invent).

**Why:** World-space bake from GLB `(along, height)` so reef-scale flashes continue across hood, doors, and rear — wrap-around continuity rather than shared-span UV island remapping.

**Brief:** same abyssal navy turquoise/magenta reef flashes; world-bake wrap-around remaster

**Date folder:** `2026-09-05` (original drop date kept; v1 `reef-flash.png` files untouched).
**Bake:** GLB world `(along=nose→tail, heightV=roof→rocker)` via `bake-world-field.mjs` / seed 0 on every panel.
